/**
 * Forecast — Projection (Lot 1 stub)
 * Full projection logic (charge engagée + probable) will be added in Lot 3.
 * For now, re-exports capacity forecast as the main entry point.
 */

export { computeForecastCapacity } from './capacityFuture';
