/**
 * Configuration de la navigation expérimentale
 * 
 * Ce flag active le nouveau système de navigation:
 * - Hub Central après login
 * - Smart Sidebar intelligente (remplace UnifiedSidebar + IconNavBar)
 * - Command Palette (⌘K / Ctrl+K)
 * 
 * Mettre à false pour revenir au comportement legacy.
 */
export const USE_EXPERIMENTAL_NAV = true;

/**
 * Route du hub central expérimental
 */
export const EXPERIMENTAL_HUB_ROUTE = '/hub';
