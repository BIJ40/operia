/**
 * Alertes Échéances (popup) — DÉSACTIVÉ.
 *
 * Suite aux retours, ce composant est volontairement neutralisé pour éviter
 * tout affichage / clignotement.
 */

export function DeadlineAlertPopup() {
  return null;
}
